name: Deploy Django App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout del código
        uses: actions/checkout@v3
        with:
          token: ${{ gitea.token }}

      - name: Construir la imagen de Docker
        run: |
          docker build -t productivity_back:latest .
      - name: Desplegar en producción vía SSH
        uses: https://github.com/appleboy/ssh-action@v1.0.3
        with:
          host: 192.168.1.10
          username: devjdtpserver
          key: ${{ secrets.SSH_PRIVATE_KEY_1 }}
          port: 22
          debug: true
          script: |
            cd /home/devjdtpserver/Compartido/Productions/productivity-prod
            /usr/bin/docker compose -f docker-compose.prod.yml up -d --build
            /usr/bin/docker compose -f docker-compose.prod.yml exec -T web python manage.py migrate --noinput
            /usr/bin/docker compose -f docker-compose.prod.yml exec -T web python manage.py collectstatic --noinput
